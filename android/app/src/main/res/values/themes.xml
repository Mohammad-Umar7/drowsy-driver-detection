<resources>
    <style name="Theme.Drowsy" parent="Theme.Material3.Dark.NoActionBar">
        <item name="android:statusBarColor">@android:color/black</item>
        <item name="android:navigationBarColor">@android:color/black</item>
        <!-- Slate rather than Material's default purple, to sit quietly under
             a camera preview. The overlay uses the same palette. -->
        <item name="colorPrimary">#7CD08E</item>
        <item name="colorOnPrimary">#0E1016</item>
        <item name="colorSecondaryContainer">#2A3140</item>
        <item name="colorOnSecondaryContainer">#E6ECF2</item>
        <item name="colorSurface">#0E1016</item>
    </style>

    <!-- Compact tonal button: equal width in a row of four, no inset so the
         row is exactly as tall as it looks. -->
    <style name="Widget.Drowsy.Button" parent="Widget.Material3.Button.TonalButton">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_weight">1</item>
        <item name="android:layout_marginStart">4dp</item>
        <item name="android:layout_marginEnd">4dp</item>
        <item name="android:insetTop">0dp</item>
        <item name="android:insetBottom">0dp</item>
        <item name="android:minHeight">44dp</item>
        <item name="android:paddingStart">4dp</item>
        <item name="android:paddingEnd">4dp</item>
        <item name="android:textSize">13sp</item>
        <item name="android:maxLines">1</item>
        <item name="cornerRadius">14dp</item>
    </style>
</resources>
